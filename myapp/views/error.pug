doctype html
html(lang='en')
  include ./common/head.pug
  +head("Starter MVP")
  body
    block content
      h1= message
      h2= error.status
      pre #{error.stack}

  include ./common/footer.pug
  +footer()
